/** @type {import('tailwindcss').Config} */
export default {
    content: [
        "./src/**/*.{html,js,jsx}", // 프로젝트 파일 경로
    ],
    theme: {
        extend: {
            fontFamily: {
                sans: ['Pretendard'],
            },
        },
    },
    plugins: [
    ],
};
